# Ensure that package-lock.json is generated
package-lock=true

# Ensure that the package-lock.json is not ignored
save-exact=true

# Ensure that the package-lock.json is not ignored
save-prefix=''

# Ensure that the package-lock.json is not ignored
save-dev=true

# Ensure that the package-lock.json is not ignored
save-optional=true

# Ensure that the package-lock.json is not ignored
save-peer=true

# Ensure that the package-lock.json is not ignored
save-bundle=true

# Ensure that the package-lock.json is not ignored
save=true

# Ensure that the package-lock.json is not ignored
save-prod=true

# Ensure that the package-lock.json is not ignored
save-dev=true

# Ensure that the package-lock.json is not ignored
save-optional=true

# Ensure that the package-lock.json is not ignored
save-peer=true

# Ensure that the package-lock.json is not ignored
save-bundle=true

# Ensure that the package-lock.json is not ignored
save=true

# Ensure that the package-lock.json is not ignored
save-prod=true 